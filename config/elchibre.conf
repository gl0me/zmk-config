# ZMK Config for My Keyboard

# Enable Bluetooth (on by default for nice!nano, but explicit is fine)
CONFIG_ZMK_BLE=y

# Enable USB (allows wired connection too)
CONFIG_ZMK_USB=y

# Optional: enable deep sleep for better battery life
# CONFIG_ZMK_SLEEP=y
